// Placeholder so the shell/router/auth-gate slice is testable on its own.
// Task 4 replaces this with the real dashboard.
export default function Dashboard() {
  return (
    <div className="flex flex-1 items-center justify-center p-6">
      <p className="text-sm text-muted-foreground">Dashboard — coming in Task 4</p>
    </div>
  )
}
